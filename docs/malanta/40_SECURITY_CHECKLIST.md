# Malanta — רשימת תיוג אבטחה

מסודר לפי **מתי** צריך לעשות כל דבר, לא לפי קטגוריה.
פריט שמסומן ⛔ הוא כזה שיקר לתקן אחר כך.

---

## שלב 0 — לפני הקוד הראשון

- [ ] ⛔ **שני פרויקטי Supabase**: `malanta-dev` (free) + `malanta-prod` (Pro).
      לא כדי שיהיה נוח — כדי שסקריפט ייבוא שירוץ על הפרויקט הלא נכון
      לא ימחק תוכן של לקוח משלם.
- [ ] ⛔ **domain restriction על מפתח האריחים** — MapTiler/Mapbox, ברגע יצירת המפתח.
      המפתח הוא `NEXT_PUBLIC_*`, כלומר גלוי לכולם. בלי הגבלה, כל אתר
      יכול לחייב את הכרטיס שלך.
- [ ] תקרת שימוש + התראת מייל אצל ספק האריחים.
- [ ] Spend Management ב-Vercel עם תקרה (כבר במסמך המקורי).
- [ ] ⛔ **החלטת R2**: bucket פרטי, object keys אקראיים, jsonb ולא text[].
- [ ] `.gitignore` כולל `.env*.local` — **בקומיט הראשון**, לא אחריו.
- [ ] `gitleaks` כ-pre-commit hook.
- [ ] שאלה לספק הסליקה: פרמטר מותאם ב-callback? (ראה `30_PAYMENTS.md`)

## שלב 1 — תשתית

- [ ] ⛔ משתני סביבה ב-Vercel: `SUPABASE_SERVICE_ROLE_KEY`, `R2_SECRET_ACCESS_KEY`,
      `RESEND_API_KEY`, מפתחות סליקה → **Production בלבד**.
      Preview deployments רצים על קוד שלא נבדק, עם אותם משתנים כברירת מחדל.
- [ ] `lib/supabase/admin.ts` — שורה ראשונה `import 'server-only'`.
- [ ] ESLint `no-restricted-imports` שאוסר `lib/supabase/*` מחוץ ל-`lib/data/**`.
- [ ] Security headers ב-`next.config.js`: CSP, HSTS, `X-Frame-Options: DENY`,
      `Referrer-Policy: strict-origin-when-cross-origin`.
      ⚠️ MapLibre צריך `worker-src blob:` ו-`img-src` לדומיין האריחים ול-R2.
- [ ] Sentry + redaction לשדות `card`/`cvv`/`pan`/`token`/`password`.

## שלב 2 — סכמה ו-RLS

- [ ] ⛔ Composite FK בין `locations`↔`maps`↔`layers` (A1)
- [ ] ⛔ Composite FK ב-`trip_stops` ו-`favorites` (A2)
- [ ] ⛔ `platform_admins` במקום `profiles.is_platform_admin` (A3)
- [ ] ⛔ `revoke update on profiles` + `grant update (עמודות מפורשות)`
- [ ] ⛔ Partial unique index על כל unique שמתלווה ל-`deleted_at` (A4)
- [ ] כל policy מסוג `for all`/`for update` — גם `using` וגם `with check` (B2)
- [ ] כל פונקציית `security definer` — `set search_path = public, pg_temp`
- [ ] `force row level security` על כל הטבלאות
- [ ] אינדקס לכל עמודה שמופיעה במדיניות
- [ ] ⛔ **חבילת בדיקות RLS ב-CI** (השלד ב-`20_RLS.sql` חלק 14)

## שלב 3 — ייבוא

- [ ] סקריפט הייבוא רץ עם service role → **חייב לכתוב ל-`audit_log`**
- [ ] דגל `--dry-run` שמדפיס ולא כותב. הרצה ראשונה תמיד dry-run
- [ ] הסקריפט מקבל `tenant_id` כארגומנט **מפורש**, אף פעם לא "האחרון שנוצר"
- [ ] אימות שכל `tenant_id`+`map_id` בקובץ הפלט עקביים לפני commit
- [ ] KML הוא XML מקובץ חיצוני → **להשבית XXE** בפרסר. `libxmljs`/`fast-xml-parser`
      עם entity expansion כבוי
- [ ] הגבלת גודל קובץ ומספר placemarks

## שלב 4 — תצוגת מפה

- [ ] תמונות: bucket פרטי + signed URL קצר-מועד אחרי `has_map_access` (B5)
- [ ] מפתחות אובייקט אקראיים (uuid), לא רציפים
- [ ] ⛔ הסרת EXIF בהעלאה — GPS בתמונה של מטייל מסגיר איפה הוא גר
- [ ] בדיקת content-type לפי magic bytes, לא לפי סיומת
- [ ] מגבלת גודל ומספר תמונות לנקודה (בסכמה: 12, 8KB metadata)
- [ ] ⚠️ אם יש מדיניות `anon` זמנית לדמו — **תזכורת מפורשת להסיר בשלב 5**

## שלב 5 — אימות ופאנל יוצר

- [ ] Rate limiting: התחברות, magic link, שחזור סיסמה
- [ ] הודעה אחידה בהרשמה — "שלחנו מייל" גם כשהמייל כבר קיים (anti-enumeration)
- [ ] `lib/errors.ts`: `42501`→"לא נמצא", `23505`→הודעה גנרית, אחר→500 גנרי
- [ ] Resend: SPF + DKIM + DMARC על הדומיין. בלעדיהם המיילים ילכו לספאם,
      וזה ייראה כמו באג במערכת
- [ ] הזמנת חבר צוות: token חד-פעמי עם תפוגה, לא uuid שמנחשים

## שלב 6 — עריכה רוחבית של שכבות

- [ ] ⛔ מדיניות `layers` למטייל = דרך `locations`, לא לפי `tenant_id` (B1)
- [ ] בדיקה: מטייל שקנה מפה אחת לא מקבל את שמות השכבות של מפה שלא קנה

## שלב 7 — הענקת גישה

- [ ] `grantAccess` בחתימה המלאה כולל `sourceRef` (ראה `30_PAYMENTS.md` §8)
- [ ] `revokeAccess` נכתבת באותו יום, לא אחר כך
- [ ] `list_map_customers` כ-RPC, זורק `not_found` ולא `forbidden` (B4)
- [ ] בדיקה: `list_map_customers` על מפה של יוצר אחר מחזיר בדיוק אותה
      שגיאה כמו מפה שלא קיימת
- [ ] הענקת גישה + ביטול → שורה ב-`audit_log`

## שלב 8 — פעילות מטייל

- [ ] בדיקה: `trip_stop` עם `location` ממפה אחרת נכשל **ברמת ה-DB**
- [ ] פקיעת זכאות: `trips` שורדות, `locations` נחסמות, ה-UI מציג
      "התוכן לא זמין — לחידוש גישה…" ולא קורס

---

## לפני הלקוח המשלם הראשון

- [ ] ⛔ **Supabase Pro** — גיבויים. ולעשות **שחזור-ניסיון פעם אחת.**
      גיבוי שלא ניסית לשחזר הוא הנחה, לא גיבוי
- [ ] מדיניות פרטיות + תנאי שימוש בעברית
- [ ] הסכם מול יוצר: מי בעל התוכן, מי בעל רשימת הלקוחות, מה קורה בעזיבה
- [ ] מימוש ייצוא + מחיקה ליוצר (כלל 15) — וגם למטייל (זכות חוקית)
- [ ] Vercel Pro (Hobby אסור מסחרית — כבר מזוהה במסמך)
- [ ] נוהל: מה עושים כשמתגלה דליפה. מי מודיע, למי, תוך כמה זמן
- [ ] ⛔ **גיבוי ידני ל-Glender** — הסיכון הפתוח שכבר מזוהה במסמך המקורי,
      ולא תלוי ב-Malanta בכלל

---

## שלוש טעויות שיקרו — ואיך לזהות אותן

**1. "רק אעקוף עם service role כדי לבדוק"**
זה נשאר. תמיד. הסימן: `import { admin }` בקובץ שלא ב-`lib/services/admin/`.
ה-ESLint rule נועד בדיוק לזה, ולכן הוא לא "נחמד שיהיה".

**2. מדיניות `anon` זמנית לדמו שנשארה**
בשלב 2–4 יש מפה ציבורית לבלוגר. אחר כך היא נשכחת ופתאום כל מפה שפורסמה
קריאה לכולם. **לרשום את ההסרה כמשימה בשלב 5, לא כתזכורת בראש.**

**3. `select *` על `profiles`**
יעבוד מצוין בפיתוח (יש לך שורה אחת) ויחזיר את כל המשתמשים בייצור אם ה-policy
פספסה. הכלל: כל שאילתה שנוגעת ב-`profiles` — עמודות מפורשות, תמיד.

---

## מבחן האמת (מתוך CLAUDE.md, כלל 1)

> "להשבית את בדיקות ההרשאה בקוד ולוודא שכלום לא דולף."

זו הבדיקה הנכונה. **להפוך אותה לבדיקה אוטומטית, לא לתרגיל מחשבתי:**
בדיקה שרצה עם client שמשתמש ב-anon key בלבד, בלי שום לוגיקת אפליקציה,
ומנסה לקרוא כל טבלה כשלושת סוגי המשתמשים. אם היא ירוקה — RLS מחזיקה.
