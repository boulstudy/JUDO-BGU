import JudoTrainer from '../JudoTrainer';

export const metadata = {
  title: 'תצוגת הקרנה — Judo BGU',
  manifest: '/manifest.json',
};

export default function DisplayPage() {
  return <JudoTrainer />;
}
